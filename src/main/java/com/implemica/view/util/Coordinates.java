package com.implemica.view.util;

/**
 * The class needed for make a resize action.
 */
public enum Coordinates {

   /** Actions are done in X plane. */
   X,

   /** Actions are done in Y plane. */
   Y
}
